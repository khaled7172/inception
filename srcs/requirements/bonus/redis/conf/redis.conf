bind 0.0.0.0
protected-mode no
port 6379
# Not reachable from outside the docker network (no published port),
# so leaving it unauthenticated inside the private network is acceptable
# for this project. Add "requirepass" + a secret if you want to harden it.
